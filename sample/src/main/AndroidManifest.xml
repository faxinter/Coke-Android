<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.coke.android.sample" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.ProgressActivity"
            android:label="@string/title_activity_progress_activity" >
        </activity>
        <activity
            android:name=".activities.NetActivity"
            android:label="@string/title_activity_net" >
        </activity>

        <!-- 友盟 -->
        <meta-data
            android:name="UMENG_APPKEY"
            android:value="54be0fbbfd98c53a0d000cab" />
        <meta-data
            android:name="UMENG_CHANNEL"
            android:value="dev" />
        <!-- 友盟 -->

    </application>

</manifest>
